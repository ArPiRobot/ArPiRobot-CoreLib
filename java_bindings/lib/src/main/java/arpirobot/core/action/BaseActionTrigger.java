package arpirobot.core.action;

import arpirobot.DestroyableObject;

/**
 * Generic action trigger. Triggers are registered with the action manager. When some event occurs
 * a trigger will run the designated action.
 */
public abstract class BaseActionTrigger extends DestroyableObject {
    // Just a common base class. No functionality here.
}
